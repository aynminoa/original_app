<div class="container mx-auto text-center text-sm lg:text-base w-4/5 lg:w-1/3 bg-white my-14 py-5 sm:py-8 rounded-md shadow-md"> 
  
  <p class="text-lg lg:text-2xl font-semibold mb-5"><i class="fas fa-heart mr-2" style="color:red;"></i>お気に入りスポット一覧</p>
  
  <table>
    <% if @favorite_spots.any? %>
    <thead>
      <tr>
        <th>spot</th>
        <th>album</th>
        <th>投稿者</th>
      </tr>
    </thead>

    <tbody>
    <% @favorite_spots.each do |spot| %>
      <tr>
        <td><%= link_to spot.name, spot %></td>
        <td><%= link_to spot.album.title, album_path(spot.album) %></td>
        <td><%= link_to spot.album.user.name, user_path(spot.album.user) %></td>
      </tr>
    <% end %>
    <% else %>
    お気に入りしたspotはまだありません。
    <% end %>
    </tbody>
  </table>

  </div> 