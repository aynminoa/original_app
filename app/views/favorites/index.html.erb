<div class="container mx-auto text-center text-sm lg:text-base w-4/5 lg:w-3/5 bg-white my-14 px-4 md:px-8 py-8 rounded-md shadow-md">
    
  <p class="text-lg lg:text-2xl font-semibold mb-5 md:mb-10"><i class="fas fa-heart mr-2" style="color:red;"></i>お気に入りスポット一覧</p>
  
  <table class="w-full text-xs sm:text-sm md:text-base text-left mb-6">
    <% if @favorite_spots.any? %>
      <thead>
        <tr>
          <th class="sm:pr-5 pb-2 w-1/5 sm:w-2/6"><span class="border-b-2 border-yellow-300"><%= t('.spot') %></span></th>
          <th class="sm:pr-5 pb-2 w-1/5 sm:w-1/6"><span class="border-b-2 border-yellow-300"><%= t('.category') %></span></th>
          <th class="sm:pr-5 pb-2 w-1/5 sm:w-1/6"><span class="border-b-2 border-yellow-300"><%= t('.tag') %></span></th>
          <th class="sm:pr-5 pb-2 w-1/5 sm:w-1/6"><span class="border-b-2 border-yellow-300"><%= t('.album') %></span></th>
          <th class="pb-2 w-1/5 sm:w-1/6"><span class="border-b-2 border-yellow-300"><%= t('.user') %></span></th>
        </tr>
      </thead>

      <tbody>
      <% @favorite_spots.each do |spot| %>
        <tr class="border-b border-dashed">
          <td class="py-1 pr-2 sm:pr-5"><%= link_to spot.name, spot, class: "hover:underline hover:opacity-75" %></td>
          <td class="py-1 pr-2 sm:pr-5"><%= spot.category %></td>
          <td class="flex flex-col text-xs sm:text-sm pr-1 sm:pr-5">
            <% spot.tags.each do |tag| %>
            <span class="inline-block"><%= tag.name %></span>
            <% end %>
          </td>
          <td class="py-1 pr-1 sm:pr-5"><%= link_to spot.album.title, album_path(spot.album), class:"hover:underline hover:opacity-75" %></td>
          <td class="py-1"><%= link_to spot.album.user.name, user_path(spot.album.user), class:"hover:underline hover:opacity-75" %></td>
        </tr>
      <% end %>
    <% else %>
      お気に入りしたspotはまだありません。
    <% end %>
      </tbody>
  </table>

</div> 