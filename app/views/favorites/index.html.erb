<div class="container mx-auto text-center text-sm lg:text-base w-4/5 lg:w-2/5 bg-white my-14 px-4 md:px-8 py-8 rounded-md shadow-md">
    
  <p class="text-lg lg:text-2xl font-semibold mb-5 md:mb-10"><i class="fas fa-heart mr-2" style="color:red;"></i>お気に入りスポット一覧</p>
  
  <table class="mx-auto text-xs sm:text-sm md:text-base text-left mb-6">
    <% if @favorite_spots.any? %>
      <thead>
        <tr>
          <th class="sm:pr-5 pb-2 w-1/3 sm:w-2/5"><span class="border-b-2 border-yellow-300"><%= t('.spot') %></span></th>
          <th class="sm:pr-5 pb-2"><span class="border-b-2 border-yellow-300"><%= t('.tag') %></span></th>
          <th class="sm:pr-5 pb-2"><span class="border-b-2 border-yellow-300"><%= t('.album') %></span></th>
          <th class="sm:pr-5 pb-2"><span class="border-b-2 border-yellow-300"><%= t('.user') %></span></th>
        </tr>
      </thead>

      <tbody>
      <% @favorite_spots.each do |spot| %>
        <tr class="border-b border-dashed">
          <td class="underline hover:opacity-75 py-1 pr-1 sm:pr-5"><%= link_to spot.name, spot %></td>
          <td class="flex flex-col text-xs sm:text-sm pr-1 sm:pr-5">
            <% spot.tags.each do |tag| %>
            <span class="inline-block"><%= tag.name %></span>
            <% end %>
          </td>
          <td class="hover:opacity-75 py-1 pr-1 sm:pr-5"><%= link_to spot.album.title, album_path(spot.album) %></td>
          <td class="hover:opacity-75 py-1 pr-1 sm:pr-5"><%= link_to spot.album.user.name, user_path(spot.album.user) %></td>
        </tr>
      <% end %>
    <% else %>
      お気に入りしたspotはまだありません。
    <% end %>
      </tbody>
  </table>

</div> 