<p>
  <strong>Title:</strong>
  <%= @album.title %>
</p>

<p>
  <strong>Visited on:</strong>
  <%= @album.visited_on.strftime("%Y年%m月") %>
</p>

<%= link_to 'Edit', edit_album_path(@album) %> |
<%= link_to 'Destroy', @album, method: :delete, data: { confirm: 'Are you sure?' } %>|
<%= link_to 'Back', albums_path %>

<%= link_to 'スポットの作成', new_spot_path(album_id: @album.id) %>